import { Navbar, Menu, DisplayGrid } from "@/components/custom";

const Home = () => {
  return (
    <div>
      <Navbar />
      <Menu />
      <DisplayGrid />
    </div>
  );
};

export default Home;
